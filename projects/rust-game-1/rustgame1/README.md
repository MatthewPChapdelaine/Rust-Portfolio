# RustGame1

A blazing-fast Rust game prototype built with macroquad.

`cargo run` → move the circle with WASD/arrows.

---
Notes for AI agents
- Build and test with Cargo (`cargo build --release`, `cargo run --release`).
- See repository Copilot guidance at `/.github/copilot-instructions.md` for workspace conventions.
